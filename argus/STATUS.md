# ARGUS — build status

Generated by `python -m argus.status`. Everything below is checked at runtime, not asserted.

## What runs without any credentials

| Capability | Command | Needs |
|---|---|---|
| 90-day arbitrage study, 28k observations | `python -m argus.research.arbitrage_study` | nothing |
| Reopening-gap study, 832 closed sessions | `python -m argus.research.gap_study` | nothing |
| Weekend-effect significance (reports FAIL) | `python -m argus.research.significance` | nothing |
| Track 1 backtest, 16 strategies x 12 rTokens | `python -m argus.research.track1_study` | nothing |
| Challenge Mode, 13 adversarial controls | `python -m argus.eval.challenge` | nothing |
| rToken panel collector | `python -m argus.market.collector` | nothing |
| Live latency probe to Bitget (30 samples) | `python -m argus.execution.latency_probe` | nothing |
| Sub-theme coverage, resolved by import | `python -m argus.status` | nothing |

## What needs the Qwen key (present)

| Capability | Command |
|---|---|
| Sleeping-Anchor decision demo | `python demo_sleeping_anchor.py` |
| Full Track-2 desk: 4 analysts -> decision -> proof | `python demo_track2_desk.py` |
| Paper-trading cycle | `python -m argus.paper.runner --once` |
| Paper-trading report | `python -m argus.paper.runner --report` |

## What needs the NVIDIA key (present)

| Capability | Command |
|---|---|
| Model-vs-model on identical state | `python -m argus.eval.bakeoff --replays 3` |

## Venue verification — COMPLETE (2026-09-12)

The full signed path is verified end to end against Bitget. `python -m argus.execution.preflight
--order --symbol SBTCSUSDT --size 0.01`:

| Step | Result |
|---|---|
| credentials | PASS — all three present |
| public_reachable | PASS — 12 rTokens quoting |
| demo_routing | PASS — `productType=SUSDT-FUTURES`, real money: False |
| signature | PASS — accepted, 1 account row |
| order_round_trip | PASS — venue id `1482642956228374529`, state **filled**, 0.01 |

Three venue facts were found by probing, not by reading docs, and each had broken the call:

1. **Demo is a `productType`, not the `paptrading` header.** `SUSDT-FUTURES` carries `SBTCSUSDT`,
   `SETHSUSDT`, `SXRPSUSDT`. The classic header returns **40099** on a v2 account — three separate
   real keys behaved identically.
2. **Demo settles in `SUSDT`, not USDT.** A USDT margin coin is refused with **40778**.
3. **This account is `hedge_mode`**, so orders need `tradeSide`. Omitting it returns **40774**.
   The mode is now read from the venue and cached, never assumed.

**The consequence that matters for Track 2:** demo carries three crypto perpetuals and **no
rTokens**. A demo order can never be a tokenized-equity order, so the hash-chained internal ledger
remains the system of record for rToken decisions. That is a venue limitation, recorded in code.

