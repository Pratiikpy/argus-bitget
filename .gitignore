# Secrets — never committed. Credentials live outside this tree.
.secrets/
*.env
!*.env.example
*.key
*.pem

# the owner's private project instructions — never public. Leaked in once (commit 84a314d);
# removed and ignored so a future broad sync cannot re-add it silently.
CLAUDE.md

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.hypothesis/

# OS
.DS_Store
Thumbs.db
