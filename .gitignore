# Secrets — never committed. Credentials live outside this tree.
.secrets/
*.env
!*.env.example
*.key
*.pem

# Private project instructions for the coding assistant — never public.
# Ignored so a broad sync cannot add it.
CLAUDE.md

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.hypothesis/

# OS
.DS_Store
Thumbs.db

# Working documents kept in the private workspace, never published: the submission-form draft
# and the build logs. They record process, not the product, and read as notes to one person.
SUBMISSION-DRAFT.md
Activity/10_LOOP_DESIGN.md
Activity/12_DECISIONS_LOG.md
Activity/13_GOAL_PROGRESS.md
Activity/PROGRESS.md

# Installed on first use from the lockfile by the crypto_sor comparison; never committed.
node_modules/
argus/data/_grammar_comparison_tmp/
